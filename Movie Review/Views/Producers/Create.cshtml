@model Movie_Review.Models.Producer

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (User.Identity.Name == "Admin_IvanMarchenko@gmail.com")
{
    <div class="obvodka m-3 p-3">
        <h1 style="margin-top:20px">Add director</h1>

        <hr />
        <div class="row">
            <div class="col-md-4">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="ProducerName" class="control-label">Director</label>
                        <input asp-for="ProducerName" class="form-control bg-dark text-light" />
                        <span asp-validation-for="ProducerName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Add" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>

        <div>
            <a style="text-decoration: none; color:#8D34FA;" class="underscores" asp-action="Index">Back to list</a>
        </div>
    </div>
}